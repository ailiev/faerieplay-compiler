// -*- c -*-

program Test4 {

    type Key = Int<16`>;

    const PI = 4;
    
    type Point = struct {
	Int<16`> x,
	Int<16`> y
    };

    function Int<16`> f (Int<16`> x) {
        f = x * 2;
    };



    function Point main (Point p) {
        var Int<16`> k2, k3, k4;

        var Point pt;

	pt = p;

        k3 = 0;
	
        pt.x = 47;
        
        if (p.x < 16) {
            for (i = 1 to 5+PI) {
                k3 = k3 + k2;
            }

            pt.y = pt.x * 3;
        }
/*	
	else {
	    k3 = k;
	    for (i = 1 to 5)
		k3 = f(k3);
	}
*/
	k3 = pt.x + pt.y;
	
        main.x = pt.x;

    }
    

}
