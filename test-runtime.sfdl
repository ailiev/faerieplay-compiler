// -*- c -*-

program Simple {

    const WordSize = 32;
    type Word = Int<WordSize`>;

    function Word main () {
	var Word sum;
	sum = 0;
	for (i = 0 to 20) {
	    sum = sum + i;
	}

	main = sum;
    }
    
}
