program SimplestCond
{

    const WordSize = 32;
    type Word = Int<WordSize>;

    function Word sfdlmain (Word x, Word y)
    {
      print ("y = ", y);
      if (y != 0) {
	sfdlmain = x/y;
      }
      else {
	sfdlmain = x;
      }
    }

}
